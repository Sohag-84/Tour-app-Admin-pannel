import 'package:flutter/material.dart';

class SelfTourPackage extends StatelessWidget {
  const SelfTourPackage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.orange,);
  }
}